# CorridorKey – ComfyUI custom node dependencies
# NOTE: torch & torchvision are NOT listed here on purpose.
# ComfyUI already ships its own CUDA-optimised PyTorch build;
# re-declaring them here would force pip to overwrite it.

# Core inference
timm>=0.9               # Hiera backbone (hiera_base_plus_224) + features_only support
numpy
opencv-python>=4.0      # cv2.connectedComponentsWithStats requires OpenCV 3+; 4.x is the modern line
tqdm

# GVM alpha-hint generator (optional at runtime, but needed if user picks GVM)
diffusers
transformers
accelerate
peft
safetensors
einops
Pillow
huggingface-hub
